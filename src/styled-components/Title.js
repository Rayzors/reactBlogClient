import styled from 'styled-components';

export const Title = styled.h1`
  font-size: 36px;
  font-weight: bold;
  margin: 1em 0;
  color: #000;
  text-align: left;
`;

export const SubTitle = styled.h2`
  font-size: 24px;
  font-weight: bold;
  margin: 1em 0;
  color: #000;
  text-align: left;
`;
